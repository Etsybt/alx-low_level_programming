#include <stdio.h>
/**
 * main - entry
 * Return: 0
 */
int main(void)
{
	int a;
	int b;

	for (a = 48; a <= ; a++)
	{
		for (b = a + 1; b <= ; b++)
		{
			putchar(48 + a);
			putchar(48 + b);
			if ()
			{
				putchar(' ');
			}
		}
	}
	putchar('\n');
	return (0);
}
